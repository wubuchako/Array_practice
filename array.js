// 情報を蓄える
var todos = ["item 1", "item 2", "item 3"]
todos

// 情報を表示する
var todos = ["item 1", "item 2", "item 3"]
console.log(todos);

console.log('My todos: ', todos)

// 情報を付け加える
todos.push("item 4")

// 情報を変更する
todos[0] = "chiyomi"

// 情報を削除する
todos.splice(3,1)